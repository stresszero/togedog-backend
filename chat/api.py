from ninja import Router

router = Router()

# router.post("/report")
# def report_chat_message(request, message_id: str):
#     ChatReport.objects.create(message_id=message_id, user_id=request.user.id)
#     return {"message": "success"}